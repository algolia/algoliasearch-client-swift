// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on
// https://github.com/algolia/api-clients-automation. DO NOT EDIT.

import Foundation
#if canImport(AlgoliaCore)
    import AlgoliaCore
#endif

public struct DomainCatalog: Codable, JSONEncodable {
    /// Fields usable as metrics.
    public var metrics: [CatalogEntry]
    /// Fields usable as filters.
    public var filters: [CatalogEntry]
    /// Fields usable in `groupBy`.
    public var groups: [CatalogEntry]
    /// Fields usable in distributions.
    public var distributions: [CatalogEntry]

    public init(
        metrics: [CatalogEntry],
        filters: [CatalogEntry],
        groups: [CatalogEntry],
        distributions: [CatalogEntry]
    ) {
        self.metrics = metrics
        self.filters = filters
        self.groups = groups
        self.distributions = distributions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case metrics
        case filters
        case groups
        case distributions
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.metrics, forKey: .metrics)
        try container.encode(self.filters, forKey: .filters)
        try container.encode(self.groups, forKey: .groups)
        try container.encode(self.distributions, forKey: .distributions)
    }
}

extension DomainCatalog: Equatable {}

extension DomainCatalog: Hashable {
    public func hash(into hasher: inout Hasher) {
        hasher.combine(self.metrics.hashValue)
        hasher.combine(self.filters.hashValue)
        hasher.combine(self.groups.hashValue)
        hasher.combine(self.distributions.hashValue)
    }
}
