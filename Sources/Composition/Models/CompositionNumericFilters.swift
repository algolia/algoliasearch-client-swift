// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on
// https://github.com/algolia/api-clients-automation. DO NOT EDIT.

import Foundation
#if canImport(Core)
    import Core
#endif

/// Filter by numeric facets.  **Prefer using the `filters` parameter, which supports all filter types and combinations
/// with boolean operators.**  You can use numeric comparison operators: `<`, `<=`, `=`, `!=`, `>`, `>=`. Comparisons
/// are precise up to 3 decimals. You can also provide ranges: `facet:<lower> TO <upper>`. The range includes the lower
/// and upper boundaries. The same combination rules apply as for `facetFilters`.
public enum CompositionNumericFilters: Codable, JSONEncodable, AbstractEncodable {
    case arrayOfCompositionNumericFilters([CompositionNumericFilters])
    case string(String)

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case let .arrayOfCompositionNumericFilters(value):
            try container.encode(value)
        case let .string(value):
            try container.encode(value)
        }
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = try? container.decode([CompositionNumericFilters].self) {
            self = .arrayOfCompositionNumericFilters(value)
        } else if let value = try? container.decode(String.self) {
            self = .string(value)
        } else {
            throw DecodingError.typeMismatch(
                Self.Type.self,
                .init(
                    codingPath: decoder.codingPath,
                    debugDescription: "Unable to decode instance of CompositionNumericFilters"
                )
            )
        }
    }

    public func GetActualInstance() -> Encodable {
        switch self {
        case let .arrayOfCompositionNumericFilters(value):
            value as [CompositionNumericFilters]
        case let .string(value):
            value as String
        }
    }
}

extension CompositionNumericFilters: Equatable {}
extension CompositionNumericFilters: Hashable {}
