// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on
// https://github.com/algolia/api-clients-automation. DO NOT EDIT.

import Foundation
#if canImport(AlgoliaCore)
    import AlgoliaCore
#endif

/// Episodic memory schema following LangMem's OTAR pattern: Observation → Thoughts → Action → Result  Captures complete
/// interaction experiences for agent learning. See
/// https://langchain-ai.github.io/langmem/concepts/conceptual_guide/#episodic-memory-past-experiences.
public struct Episode: Codable, JSONEncodable {
    /// What user wanted + key context (1-2 sentences). Include prior failed attempts if they informed the approach.
    public var observation: String
    /// WHY this approach was chosen, which constraints/preferences drove decisions (1-3 sentences). Capture reasoning
    /// that applies to similar future scenarios.
    public var thoughts: String
    /// What was done with PRECISE details (1-3 sentences). WITH tool calls: use arrow notation `tool(param:value) →
    /// feedback → tool(refined_param:new_value)`. WITHOUT tool calls: capture communication/workflow pattern.
    public var action: String
    /// Learned pattern + effectiveness (1-3 sentences). What worked and WHY it's replicable. Note efficiency:
    /// multi-turn refinements, which results were relevant, what made final attempt succeed. Use strict `param:value`
    /// syntax for learnings. Format: 'For [context], use [param:value] because [reason]'.
    public var result: String

    public init(observation: String, thoughts: String, action: String, result: String) {
        self.observation = observation
        self.thoughts = thoughts
        self.action = action
        self.result = result
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case observation
        case thoughts
        case action
        case result
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.observation, forKey: .observation)
        try container.encode(self.thoughts, forKey: .thoughts)
        try container.encode(self.action, forKey: .action)
        try container.encode(self.result, forKey: .result)
    }
}

extension Episode: Equatable {}

extension Episode: Hashable {
    public func hash(into hasher: inout Hasher) {
        hasher.combine(self.observation.hashValue)
        hasher.combine(self.thoughts.hashValue)
        hasher.combine(self.action.hashValue)
        hasher.combine(self.result.hashValue)
    }
}
