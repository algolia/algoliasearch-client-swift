// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on
// https://github.com/algolia/api-clients-automation. DO NOT EDIT.

import Foundation
#if canImport(AlgoliaCore)
    import AlgoliaCore
#endif

/// Client-supplied metadata on a v5 user message.  `turn_context` is namespaced so other callers can use `metadata` for
/// unrelated purposes without collision. Unknown keys are preserved and ignored by this pipeline.  Note: `turn_context`
/// is deliberately typed `dict[str, Any]` (not `TurnContext`). The metadata is parsed eagerly with the request body,
/// but cap/charset validation must be deferred to `extract_turn_context_v5` so the kill-switch
/// (`TURN_CONTEXT_ENABLED=false`) can silently drop payloads instead of 422-ing. See
/// `test_invalid_metadata_does_not_raise_at_model_construction`.
public struct UserMessageMetadataV5: Codable, JSONEncodable {
    public var turnContext: [String: AnyCodable]?

    public init(turnContext: [String: AnyCodable]? = nil) {
        self.turnContext = turnContext
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case turnContext
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            self.additionalProperties[key] = newValue
        }
    }

    public init(from dictionary: [String: AnyCodable]) throws {
        self.turnContext = dictionary["turnContext"]?.value as? [String: AnyCodable]

        for (key, value) in dictionary {
            switch key {
            case "turnContext":
                continue
            default:
                self.additionalProperties[key] = value
            }
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.turnContext, forKey: .turnContext)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(self.additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        self.turnContext = try container.decodeIfPresent([String: AnyCodable].self, forKey: .turnContext)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("turnContext")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        self.additionalProperties = try additionalPropertiesContainer.decodeMap(
            AnyCodable.self,
            excludedKeys: nonAdditionalPropertyKeys
        )
    }
}

extension UserMessageMetadataV5: Equatable {}

extension UserMessageMetadataV5: Hashable {
    public func hash(into hasher: inout Hasher) {
        hasher.combine(self.turnContext?.hashValue)
        hasher.combine(self.additionalProperties.hashValue)
    }
}
